<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Mapa OpenStreetMap -->
    <org.osmdroid.views.MapView
        android:id="@+id/map"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Overlay superior: texto de estado + leyenda -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="vertical"
        android:background="#CC000000">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:text="@string/stopped" />
    </LinearLayout>

    <!-- Overlay inferior: botón para iniciar/detener -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/scanButton"
        style="@style/MaperoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:text="@string/start" />

    <!-- Overlay inferior-izquierda: alternar streaming en tiempo real -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/streamButton"
        style="@style/MaperoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_margin="24dp"
        android:text="@string/stream_on"
        android:textSize="16sp" />

    <!-- Overlay lateral: seguir posición GPS -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/followButton"
        style="@style/MaperoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:layout_marginEnd="12dp"
        android:text="@string/follow_on" />

    <!-- Overlay lateral: centrar el mapa en la posición del teléfono -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/centerButton"
        style="@style/MaperoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="72dp"
        android:text="@string/center" />

</FrameLayout>
